name: First Workflow
on:
  push:
    branches:
      - master
jobs:
  MiprimerJob:
    name: deploy a msg
    runs-on: ubuntu-latest
    steps:
    - name: Install SSH key
      uses: shimataro/ssh-key-action@v2
    - name: mi primer mensaje
      run: echo "Hola mundo"
